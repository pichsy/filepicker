## FilePicker 选择角标 × Paging3 刷新协同的逻辑改造方案（保留现有UI）

目标：不改 UI 布局与样式，仅改“选择逻辑、索引维护、适配器绑定和刷新策略”，保证：
- 选中/取消/批量滑选角标稳定、最小刷新；
- 兼容分页（未来接入 Paging 3 也不乱序）；
- 不依赖 position，支持跨页面与筛选场景；
- 可逐步落地，回滚简单。

---

### 0. 范围声明
- 不改 XML 与可视样式（保持 `FilePickerItemRvAlbum*` 等布局不变）。
- 仅改 `FilePickerViewModel.kt`、`FilePickerFragment.kt` 刷新调用、以及 RecyclerView 适配器绑定逻辑。
- 若未来引入 Paging 3，可直接复用本方案的数据结构与刷新策略。

---

### 1. ViewModel：全局选择态与角标索引（角标真源）

文件：`filepicker/src/main/java/com/pichs/filepicker/FilePickerViewModel.kt`

1.1 增加全局选择结构（与分页解耦）：
- 新增字段：
  - `private val selectedPathSet = mutableSetOf<String>()`
  - `private val selectedList = mutableListOf<String>() // path 按选择顺序`
  - `private val pathToIndexMap = mutableMapOf<String, Int>() // path -> 序号`
  - `private val pathToPositionMap = mutableMapOf<String, Int>() // path -> adapter position（仅在当前列表可用，非必须，可选）`

1.2 对外只暴露只读方法：
- `fun isSelected(path: String): Boolean = selectedPathSet.contains(path)`
- `fun getSelectedIndex(path: String): Int = pathToIndexMap[path] ?: -1`
- `fun getSelectedCount(): Int = selectedList.size`

1.3 选中/取消 API（返回“最小影响集”的路径）：
- `fun selectItem(path: String): List<String>`
  - 若已选，直接 return 空；
  - append 到 `selectedList`，更新 `selectedPathSet` 与 `pathToIndexMap[path] = lastIndex`；
  - return listOf(path)。
- `fun unselectItem(path: String): Pair<String, List<String>>`
  - 若未选，return 空；
  - 取 `k = pathToIndexMap[path]`，从 `selectedList` 移除 path，`selectedPathSet.remove(path)`；
  - 遍历 `selectedList[k..end]`：对每个 `p` 执行 `pathToIndexMap[p]--`；
  - 返回 `(path, affectedAfterKPaths)`。
- `fun mergeSlideSelection(adds: List<String>, removes: List<String>): Pair<List<String>, List<String>>`
  - 合并新增与删除；新增走 `selectItem`（收集路径），删除依次走 `unselectItem` 算法（收集所有受影响路径）；
  - 返回 `(addedPaths, affectedPaths)`。

1.4 角标索引与 UI 同步时机：
- 角标的唯一真源是 ViewModel（`selectedList/selectedPathSet/pathToIndexMap`）。
- UI 不得自行“推断”角标（禁止 `indexOfSelected`/线性 contains 扫描）。
- 非 Paging：可在数据变更后重建 `pathToPositionMap`，便于按 position 定向刷新。
- Paging：不维护 positionMap，仅使用稳定 ID + “已附着 ViewHolder”直更（见第 4 节）。

---

### 2. 稳定 ID：按 MediaStore _ID 优先，路径哈希兜底

适配器需启用稳定 ID，避免依赖 position 刷新。

2.1 稳定 ID 获取：
- 优先从 `MediaEntity.uri` 提取 `_ID`（`ContentUris.parseId(uri)` 或取末段数字）。
- 若不可用，使用规范化 `path` 拼接少量稳态字段（如 size、addTime）计算 64 位哈希作为 `Long` ID。

2.2 适配器启用 stableIds：
- `recyclerView.setHasStableIds(true)`；适配器覆盖 `getItemId(position)` 返回上述 ID。

---

### 3. Adapter 绑定与 payload 精准刷新（统一协议）

文件：`FilePickerFragment.kt` 中网格与列表的 `setup { onBind { ... } }` 区域。

3.1 onBind 改造（O(1) 取角标与选中态）：
- 通过 `val path = item.path.orEmpty()`；
- 角标：`val index = viewModel.getSelectedIndex(path)`；
- 是否选中：`viewModel.isSelected(path)`；
- UI 赋值：角标文本、选中遮罩按上述两值设置，不再调用 `indexOfSelected(item)` 或扫描 `selectedData`。

3.2 onPayload 精准更新：
- 定义 payload：`SELECTION_CHANGED`（选中态+角标）、`INDEX_CHANGED`（仅角标）。
- 在 `onPayload` 分支中仅更新角标文本与遮罩，避免触发图片重绑与动画。

3.3 点击事件改造（与 ViewModel 最小集协作）：
- 选中：调用 `viewModel.selectItem(path)`，得到 `addedPaths`，对这些 path：
  - 若有 `pathToPositionMap`：取 position 执行 `notifyItemChanged(position, SELECTION_CHANGED)`；
  - 否则（或 Paging）用 `findViewHolderForItemId` 按 ID 更新可见项；不可见项不处理，等待其 onBind 自然正确。
- 取消：调用 `viewModel.unselectItem(path)` 得到 `(removedPath, affectedAfterKPaths)`：
  - `removedPath` 执行 `SELECTION_CHANGED`；
  - `affectedAfterKPaths` 执行 `INDEX_CHANGED`（角标-1）。

---

### 4. 批量滑动选择的合并提交（结束时一次性刷）

文件：`FilePickerRecyclerView` 回调接入 `FilePickerFragment` 的 `onTouchSelectEnd()`。

4.1 结束时统一提交：
- 从临时集合得出 `adds` 与 `removes`；
- 调用 `viewModel.mergeSlideSelection(adds, removes)` 获取 `(addedPaths, affectedPaths)`；
- 优先用 `pathToPositionMap` 定位、批量 `notifyItemChanged(..., payload)`；若无则按 ID 更新可见项。

4.2 滑动过程中不频繁刷新：
- 仅更新底部计数文本与“预览/确定”按钮状态；减少 UI 抖动。

---

### 5. Fragment 刷新调用替换（去整表，最小粒度）

文件：`FilePickerFragment.kt`

5.1 移除整表刷新：
- 删除 `updateSelectDataUI()` 中的 `notifyItemRangeChanged(0, size)`。
- “最后一行间距”交由 `ItemDecoration` 控制，移除相关范围刷新。

5.2 替换点击/预览处的刷新：
- 所有选中/取消/删除拖拽的地方改为调用第 3 节中的“最小集合”刷新策略（`SELECTION_CHANGED` / `INDEX_CHANGED`）。

---

### 6. 生命周期与重复加载（避免覆盖）

6.1 关闭重复加载：
- 移除 `onResume()` 中的 `loadData()` 调用，避免返回页面二次全量覆盖列表与角标。

6.2 后续增强（可选）：
- 通过 `ContentObserver` 监听媒体库变化并去抖刷新。

---

### 7. 验证清单与回滚

验证：
- 单选/取消：仅目标项或少量项刷新；角标稳定、无错位。
- 取消中间项：被取消项清空角标，后续已选项角标依次减一；不可见项滑回可见后角标正确。
- 批量滑选：结束后一次性刷新，滑动过程中流畅。
- Tab/相册切换：角标可见项正确；不可见项在进入视口时正确。

回滚：
- 保留旧的 `selectedData` 与整表刷新实现的开关；如遇异常可一键回退。

---

### 8. 角标 × Paging3 刷新协同的关键原则
- 角标只来源于 ViewModel 的全局映射，与分页位置无关。
- PagingDataAdapter 不做“大范围 notify”，任何刷新仅针对“已附着”的可见 ViewHolder：
  - 通过稳定 ID 寻址：`findViewHolderForItemId(id)`。
  - 可见项立即 payload 局刷；不可见项不处理，滚入视口时 onBind 自然正确。
- onBind 只做 O(1) 读取并渲染，不做任何线性推断或集合遍历。

---

### 9. Paging 3 接入改造（完整细则：从数据到刷新）

目标：接入 Paging 3 以降低首屏与大列表内存/CPU 开销，同时保证“选择角标稳定、跨页不乱”。UI 保持现状（布局/样式不改），仅更换数据驱动与适配器实现。

9.1 数据源与查询参数（QueryParams）
- 定义 `data class QueryParams(selectType, minSize, maxSize, folderIdOrPath, containsGif, ... , version:Int)`；当筛选条件变更或需要强制刷新时提升 `version`。
- `ViewModel` 暴露 `queryParamsFlow: StateFlow<QueryParams>`；Tab/相册/筛选切换时更新该 Flow。

9.2 MediaStore PagingSource 设计
- Android Q 及以上：优先使用 `ContentResolver#query` 的分页 Bundle 参数：
  - `QUERY_ARG_LIMIT`、`QUERY_ARG_OFFSET`、`QUERY_ARG_SORT_COLUMNS`（`DATE_ADDED`）与 `QUERY_ARG_SORT_DIRECTION`（DESC）。
- Android Q 以下：采用 Keyed 方案（推荐）
  - 以 `(dateAdded, _ID)` 作为复合游标键；`load(params.key)` 使用 `< lastKey` 条件继续向后加载，避免 `OFFSET`。
  - 初始 `key=null` 加载最新一页；每次返回 `nextKey = Pair(minDateAddedInPage, minIdAtThatDate)`。
- 过滤条件与 MIME/大小/隐藏目录过滤沿用 `QueryWhere.Builder`，尽量 SQL 下推，避免逐条文件系统判断。

9.3 Pager 构建
- `Pager(
    config = PagingConfig(
        pageSize = 60,
        initialLoadSize = 120,
        prefetchDistance = 60,
        enablePlaceholders = false
    ),
    pagingSourceFactory = { MediaStorePagingSource(queryParamsFlow.value) }
)`
- Flow 管道：`queryParamsFlow.distinctUntilChanged().flatMapLatest { params -> Pager(...).flow }`
- 在 `Fragment` 中 `lifecycleScope.launch` 收集并 `adapter.submitData()`。

9.4 适配器：PagingDataAdapter + 稳定 ID + ItemCallback
- 使用 `PagingDataAdapter<MediaEntity, VH>`，保留现有 ViewHolder 绑定逻辑（UI 不变）。
- `setHasStableIds(true)` 并覆盖 `getItemId(position)`：
  - 优先 `MediaStore _ID`（从 `MediaEntity.uri` 解析）；
  - 兜底：`stableHash("${path.lowercase()}|${size}|${addTime?:0}")` 转 `Long`。
- DiffUtil.ItemCallback：
  - `areItemsTheSame`：比较稳定 ID 或 `(uri/_ID || path)`；
  - `areContentsTheSame`：
    - 若角标不落地到模型，建议始终返回 true（否则会引发整页 diff 抖动），把选择刷新交由“按ID定向更新”来做；
    - 或者将 `selectedIndex` 映射进展示模型（Flow combine），则在仅角标变化时返回 false 并给出 payload。
  - `getChangePayload`：当仅角标变化返回 `INDEX_CHANGED`，当选中态变化返回 `SELECTION_CHANGED`。

9.5 onBind/onPayload 与角标来源
- onBind：
  - 通过 `val path = item.path.orEmpty()`；
  - `val idx = viewModel.getSelectedIndex(path)`，`val checked = viewModel.isSelected(path)`；
  - 按上述两值设置角标与遮罩（O(1)），不做线性扫描。
- onPayload：
  - `SELECTION_CHANGED`：更新遮罩+角标；
  - `INDEX_CHANGED`：仅更新角标文本；
  - 禁止重绑图片，避免重复解码。

9.6 选择变更后的 UI 更新（两种路径，二选一）
- 不调用 `notifyDataSetChanged` 或大范围 `notify`。
- 路径 A（推荐，侵入小）：
  - 计算受影响 path 集合（最小集算法），对“可见且已附着”的项按稳定 ID 定向更新：
    - `recyclerView.findViewHolderForItemId(itemId)?.let { holder -> holder.payloadUpdate(...) }`
  - 不可见项不处理，靠 onBind 自然正确。
- 路径 B（角标入模型）：
  - 将 `selectedIndex` 合入 PagingData 的展示模型（Flow combine + map）；
  - 选择变化时发射一个轻量 `selectionVersion`，触发已加载页的差分；
  - 由 ItemCallback 的 payload 仅更新角标 UI。

9.7 全局稳定 ID 刷新入口（统一实现）
- 封装工具 `fun refreshByStableIds(ids: Collection<Long>, payload: Any)`：
  - 遍历 id，`findViewHolderForItemId(id)?.let { bindingPayload(it, payload) }`；
  - 由 Adapter 提供 `bindingPayload(holder, payload)`，内部只更新角标与遮罩。
- ViewModel 选择 API 返回 path 集合 → 映射为 ids → 调用 `refreshByStableIds`。

9.7 Tab/相册/筛选切换
- 仅更新 `queryParamsFlow`，依赖 `flatMapLatest` 取消旧分页流并创建新 Pager。
- 选择角标来自 ViewModel 的全局映射，切换后 onBind 即正确；无需遍历全表刷新。

9.8 ContentObserver 与刷新
- 注册 MediaStore `ContentObserver`；变化时仅提高 `queryParams.version` 触发刷新（避免直接对 Adapter 调用全量刷新）。
- 对于 Q 以下的 Keyed 方案，触发刷新即新建 Pager，旧流被取消。

9.9 底部已选列表与拖拽排序
- 保持非分页普通 Adapter，使用 Diff 提交，条目 ID 使用与主列表相同的稳定 ID。
- 拖拽排序改变 `selectedList` 顺序后：
  - 重新计算 `pathToIndexMap`；
  - 仅对当前“可见且已附着”的主列表项按 ID 发送 `INDEX_CHANGED`（角标重排）。

9.10 预取与性能
- `RecyclerViewPreloader`（Glide）按滚动方向预取即将进入视口的 1~2 行缩略图。
- PagingConfig 的 `prefetchDistance` 设为一个屏幕容量（例如 60）以减少边界抖动。

9.11 错误与重试
- 使用 `LoadStateListener` 展示加载中/错误态；错误提供“重试”按钮调用 `adapter.retry()`。
- 这些 UI 组件可通过现有布局增加 Footer，不影响主列表项 UI。

9.12 验证与边界
- 大量选择/取消、取消中间项、快速滑选：观察仅目标与可见受影响项更新，角标稳定。
- 快速切换 Tab/相册：旧流被取消，无闪烁；可见项角标正确；不可见项滚入后也正确。
- 大数据设备与低端机：首屏时间、滚动帧率、内存峰值均优于非分页实现。


---

### 10. 已完成改造清单（代码已提交）
- ViewModel 全局选择索引：`selectedPathSet/selectedList/pathToIndexMap/pathToPositionMap`；新增 `selectPath/unselectPath/mergeSlideSelection/reorderSelectedByEntityListOrder`。
- Fragment/Adapter：
  - 角标与选中态统一从 ViewModel O(1) 读取；移除 `indexOfSelected` 线性计算。
  - onPayload 精准刷新：`SELECTION_CHANGED/INDEX_CHANGED`，避免重绑图片。
  - 点击、预览弹窗、底部已选删除、拖拽结束、滑动选择结束，全部改为“最小影响集 → payload”刷新。
  - 移除整表刷新与 `onResume()` 重复加载。
- 预览弹窗：统一使用新索引；底部缩略选择与主列表角标同步。
- 稳定ID：`MediaEntity.stableId()`（优先 `_ID`，path|size|addTime 哈希兜底）。
- Paging3 依赖与骨架：
  - 依赖：`androidx.paging:paging-runtime-ktx` 已加入 `filepicker/build.gradle.kts`。
  - 源码：`query/MediaStorePagingSource.kt` 与 `query/MediaStorePaging.kt`（当前实现用全量缓存切片，便于渐进迁移，后续可替换为真分页）。

注意：当前主列表仍使用 BRV，尚未切换为 PagingDataAdapter；角标与刷新逻辑已与分页解耦，随时可替换适配器。

---

### 11. 下一步：主列表切换为 PagingDataAdapter（保留现有UI）
目标：低侵入迁移，继续沿用“稳定ID直更可见项”的刷新策略。

实施步骤：
1) 适配器替换
   - 创建 PagingDataAdapter 派生适配器（沿用现有 ViewHolder 与 onBind/onPayload 逻辑）。
   - `setHasStableIds(true)`；`getItemId(position)=item?.stableId() ?: RecyclerView.NO_ID`。
   - ItemCallback：
     - `areItemsTheSame` 比较稳定ID或 `(uri/_ID||path)`；
     - `areContentsTheSame` 建议返回 true，将选择刷新交给“按ID直更”；如需入模则给出 `getChangePayload`。
2) 数据流对接
   - 在 `FilePickerFragment` 创建 `pager = MediaStorePaging.pager(context, selectType, minSize, maxSize)`；
   - `lifecycleScope.launch { pager.collectLatest { adapter.submitData(it) } }`；
   - Tab/相册/筛选切换：重建 pager（或通过 paramsFlow flatMapLatest）。
3) 列表刷新与角标
   - 选中/取消/滑选/拖拽后：
     - 计算“新增集/受影响集”→ 映射为 `stableId`；
     - 遍历 ids：`recyclerView.findViewHolderForItemId(id)?.let { holder -> holder.payloadUpdate(...) }`；
     - 不可见项不处理，进入视口时 onBind 自然正确。
4) 底部已选列表保持普通 Adapter（Diff 提交）；拖拽结束后调用 `reorderSelectedByEntityListOrder()` 并局部刷主列表可见项角标。
5) 内容观察与刷新（可选）：
   - 注册 `ContentObserver`；变化时重建 pager 或 bump `version`。

---

### 12. 影响面与回滚
- 影响面：列表适配器替换（BRV → PagingDataAdapter），数据源从一次性集合改为 PagingData；其余 UI/交互不变。
- 回滚：保留原 BRV 适配器实现与一次性加载路径的开关，异常时可快速切回。

---

### 13. 验收与度量
- 功能：单选/取消、取消中间项角标递减、滑动选择、预览页选择、底部列表删除与拖拽，均仅局部刷新且状态一致。
- 性能：
  - 首屏：列表可见 < 300ms；首屏缩略图稳定 < 800ms（中端机）。
  - 滚动：> 55fps；滑选结束后一帧内完成局刷。
  - 内存：对比一次性加载，峰值下降 ≥ 15%。


