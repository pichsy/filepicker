## FilePicker 优化方案（分阶段、可落地）

说明：本文基于 `anylise.md` 的结论与当前代码实现，输出可直接执行的优化步骤，并解释“为什么”与“验证方式”。不包含代码实现，仅步骤与理由。

### 阶段划分
- 第一阶段（快速收益，1~2 天）：降低 IO/日志开销，精简刷新范围，首屏可见更快
- 第二阶段（结构升级，3~5 天）：分页/增量、异步排序与缓存、稳定 ID + 精准刷新
- 第三阶段（体验完善，2~3 天）：内容观察去抖、预取与 Glide 调优、监控与回滚

---

### 第一阶段：快速收益

1) 精简 MediaStore 遍历中的文件系统调用与日志
   - 步骤：
     - 在 `FileQueryHelper.queryAlbums(...)` 的 Cursor 循环中，移除/关闭 per-row 日志（`Log.d/e`），仅保留阶段性摘要。
     - 依赖 `MediaStore.Files.SIZE` 字段做大小过滤，移除 `isFileExists/isFile/getFileSize` 等逐条文件系统调用。
     - 将隐藏目录过滤下推到 SQL：在 `QueryWhere.Builder` 组合条件中加入 `DATA NOT LIKE '%/.%'`（或更精确的目录段匹配）。
   - 原因：
     - 日志与文件系统调用在大列表下会拖慢 IO 线程，延迟 `fastNumber` 首次回调；日志 I/O 也会影响主线程。
   - 验证：
     - 记录“调用 `queryAlbums` 到 `onFastCallBack`”的时间；期望减少 30%~60%。

2) 强化“快速回调”与首屏绑定
   - 步骤：
     - 将 `fastNumber` 从 40 提升到 60~100（按设备性能微调）。
     - 首次回调仅构建“全部数据列表”（当前相册或默认“全部”），不急于构建所有 `MediaFolder` 的完整数据。
   - 原因：
     - 首屏 2~3 屏可见可减少二次数据抖动，用户感知更好。
   - 验证：
     - 统计首次回调到“列表首屏渲染完成”的时间；目标 < 800ms（中端机）。

3) onBind 降噪 + Glide 轻量缩略图
   - 步骤：
     - 统一缩略图尺寸：`override(160~200)`，并关闭不必要的 transform/animate。
     - GIF 缩略图首屏使用静态首帧（`asBitmap()`）或仅显示“GIF”角标，动图在预览中播放。
   - 原因：
     - 降解码与上传纹理成本，避免 payload 触发重解码卡顿。
   - 验证：
     - 滚动流畅度提升，掉帧显著下降（> 55fps）。

4) 去除整表刷新：改为最小粒度刷新
   - 步骤：
     - 移除 `updateSelectDataUI()` 内的 `notifyItemRangeChanged(0, size)`。
     - 单项点击选中/取消：仅 `notifyItemChanged(modelPosition, payload=SELECTION_CHANGED)`。
     - “最后一行底部间距”改由 `ItemDecoration` 控制，不再 `notifyItemRangeChanged(lastRow...)`。
   - 原因：
     - 整表 `notify` 触发全量 measure/layout，造成卡顿。
   - 验证：
     - 单次点击后主线程耗时显著降低；trace 中大范围 rebind/remeasure 消失。

5) 关闭 `onResume()` 自动 reload
   - 步骤：
     - 在 `FilePickerFragment.onResume()` 中移除二次 `loadData()`。
   - 原因：
     - 规避初始化期间或返回页面时的重复加载与 UI 抖动。
   - 验证：
     - 返回页面不卡；选择角标不被覆盖。

---

### 第二阶段：结构升级

6) 引入选择角标的精细刷新（ViewModel + Adapter 协同）
   - 步骤：
     - 在 ViewModel 中增加：
       - `selectedPathSet: MutableSet<String>`、`pathToIndexMap: MutableMap<String, Int>`、`pathToPositionMap: MutableMap<String, Int>`。
       - 设置/变更 `currentFolderDataList` 时重建 `pathToPositionMap`。
       - 选中/取消/批量合并 API：返回需要 `notifyItemChanged` 的 positions 及 payload 类型（`SELECTION_CHANGED`/`INDEX_CHANGED`）。
     - Adapter：
       - 开启稳定 ID（或迁移 `ListAdapter`/`AsyncListDiffer`），Diff 的 `areContentsTheSame` 纳入 `selectedIndex`。
       - onPayload 仅更新角标与遮罩，不重绑图片。
   - 原因：
     - 将刷新粒度缩小到“最小受影响集”，显著降低主线程负担。
   - 验证：
     - 单选/取消仅 1~若干项刷；批量滑选在结束时一次性提交变更；滚动与多选无明显卡顿。

   - 角标索引维护与刷新细则（关键补充）：
     - 数据结构：
       - `selectedList: MutableList<String>`（按选择顺序保存 path，等价于角标序列）；
       - `selectedPathSet` 用于 O(1) 判定是否选中；
       - `pathToIndexMap` 保存 path 在 `selectedList` 的序号（角标 = index + 1）；
       - `pathToPositionMap` 保存 path 在当前 Adapter 中的位置，用于定位最小刷新项。
     - 选中单项（pathP）：
       1) `selectedList.add(pathP)`；
       2) `selectedPathSet.add(pathP)`；
       3) `pathToIndexMap[pathP] = selectedList.lastIndex`；
       4) 找到 `position = pathToPositionMap[pathP]`，`notifyItemChanged(position, SELECTION_CHANGED)`（含角标与遮罩）；
       5) 不影响其他项角标，无需额外刷新。
     - 取消单项（pathK）：
       1) 取 `k = pathToIndexMap[pathK]`，从 `selectedList` 移除 pathK；
       2) `selectedPathSet.remove(pathK)`；
       3) 从 k 开始遍历 `selectedList[k..end]`：对每个 `pathI` 执行 `pathToIndexMap[pathI]--`；
       4) 受影响集合 S = `{pathK} ∪ selectedList[k..end]`；
       5) 将 S 映射为 positions，并：
          - 对 `pathK` 执行 `notifyItemChanged(positionK, SELECTION_CHANGED)`（去遮罩与清空角标）；
          - 对其余（k 后）执行 `notifyItemChanged(positionI, INDEX_CHANGED)`（仅角标数字减一）。
     - 批量滑选：
       - 结束时合并新增集 A 与删除集 D；
       - 对 A 按当前 `selectedList` 顺序 append（或按需要插入），逐项 `SELECTION_CHANGED`；
       - 对 D 依次执行“取消单项”算法，自动产生“后续角标递减”的最小刷新集；
       - 合并 positions 去重后批量 `notify`，避免重复刷新。
     - Tab/相册切换：
       - 重建 `pathToPositionMap`；
       - 若 UI 需要渲染角标（例如过滤后仍在当前数据集中的已选项），对这些 path 定位 position 做一次 `INDEX_CHANGED` 以保证可见性正确；不对缺席项刷新。
     - Adapter onBind/onPayload：
       - onBind：O(1) 读取 `selectedPathSet.contains(path)`、`pathToIndexMap[path]` 设置遮罩与角标，不做线性搜索；
       - onPayload：
         - `SELECTION_CHANGED`：更新遮罩 + 角标（可能为清空）；
         - `INDEX_CHANGED`：仅更新角标文本；
       - 禁止在 payload 分支重绑图片，以免触发重解码。

7) 排序与过滤放到后台并缓存结果
   - 步骤：
     - 在 IO 线程完成 `sortedByDescending(addTime)`，再通过 StateFlow 一次性提交 UI。
     - 对不同 Tab（图片/视频/GIF）建立缓存；切换 Tab 优先使用缓存结果。
   - 原因：
     - 避免主线程排序；切换 Tab 不再重复全表筛选与排序。
   - 验证：
     - Tab 切换无明显卡顿；trace 中无主线程长耗时排序。

8) 分页/增量加载（Paging 3）
   - 步骤：
     - 基于 `MediaStore` 封装 `PagingSource`；首页加载 60 条，列表底部触发加载更多。
     - 相册选择弹窗按需分页加载，减少一次性内存与构建成本。
   - 原因：
     - 大数据集下显著降低首次内存占用与渲染压力。
   - 验证：
     - 首屏出现更快；内存峰值与 CPU 占用下降。

9) 底部已选列表使用 Diff 刷新
   - 步骤：
     - 将 `binding.rvSelected.models = ...` 改为基于 Diff 的 `submitList`。
   - 原因：
     - 避免整表替换，提升交互时的流畅度。
   - 验证：
     - 删除/拖拽排序时仅局部刷新，不卡顿。

---

### 第三阶段：体验完善

10) MediaStore 内容观察 + 去抖刷新
    - 步骤：
      - 注册 `ContentObserver` 监听相册变更，500~1000ms 去抖后触发数据刷新（仅增量差异）。
    - 原因：
      - 避免手动 reload；在变化频繁时减少重复刷新。
    - 验证：
      - 连续拍照/删除场景不卡顿；数据能及时更新。

11) Glide 深度调优与预取
    - 步骤：
      - 统一 `format(PREFER_RGB_565)`、`thumbnail(0.25f)`、`downsample(AtMost)`；按设备能力限制并发请求数。
      - 在滚动监听中对“即将进入视口”的 1~2 行做 `preload`。
    - 原因：
      - 降低单帧内的解码与上传峰值；平滑滚动体验。
    - 验证：
      - 滚动掉帧减少；Jank 指标收敛。

12) 监控与回滚
    - 步骤：
      - 加埋点：首帧时间、首次可见、列表绑定耗时、onBind 平均耗时、滑动帧率、GC 次数/时间、失败率。
      - 增加调试开关：性能日志开关、GIF 动图开关、分页大小等；支持快速回滚。
    - 原因：
      - 可度量、可回退，便于灰度与问题定位。
    - 验证：
      - 指标面板稳定，异常可快速关闭。

---

### 风险与回退点
- 稳定 ID/Diff 行为变更需验证边界：数据源替换、Tab 切换、相册切换、批量选中/取消。
- SQL 过滤过于激进可能漏数据；先灰度到部分机型/系统版本。
- GIF 静态化影响预期体验；预览界面保留动图播放以兜底。

### 成功验收标准（建议）
- 首屏：列表出现 < 300ms；首屏缩略图稳定 < 800ms（中端机）
- 滚动：> 55fps；长列表滑动无明显丢帧
- 多选：快速滑选结束后一帧内完成批量刷新
- 内存：峰值下降 15%+；频繁操作无 OOM/ANR


